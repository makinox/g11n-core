const networkOrigin = 'http://localhost:4200/';
enum stringSeparators {
  PERCENTAGE = '%',
  AMPER = '&',
}
enum errorMessages {
  NOT_FOUND = 'Not found',
}

export { networkOrigin, errorMessages, stringSeparators };
